#!/system/bin/sh
#!/usr/bin/env sh

#
# find -ls
#

l="./"
ls -1R | while read line; do
	case "$line" in
		*:) # pushd by ':'
			l="${line%:*}/"
			ls -lids "$l"
		;;
		?*)
			ls -lids "${l}${line}"
			#echo "${l}${line}"
		;;
		*) # popd by empty line
			l="./"
		;;
	esac
done

